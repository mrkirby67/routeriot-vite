{
  "name": "routeriot-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy",
    "go": "[ -d .git ] || { echo '❌ Not in a Git repo!'; exit 1; }; npm run deploy && git add -A && git commit -m \"🔄 Auto-sync on $(date '+%Y-%m-%d %H:%M:%S')\" || echo '⚠️ No changes to commit' && git push origin main && echo '✅ Firebase + GitHub Sync Complete 🚀'"
  },
  "devDependencies": {
    "vite": "^5.3.1"
  }
}