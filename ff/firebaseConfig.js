// /ff/firebaseConfig.js
// Reuse the existing Route Riot Firebase config to avoid duplication.
// Do NOT hardcode keys here.

import { firebaseConfig } from '../core/config.js';

export { firebaseConfig };
